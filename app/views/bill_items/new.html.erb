<% provide :page_title, "Add item to bill #{@bill_item.bill.id}" %>
<% provide :page_heading, "Add item to bill #{@bill_item.bill.id}" %>

<menu class="col-xs-12 action-bar">
  <div class="text-left col-xs-12 col-md-4 action-bar-section">
    <%= link_to shift_url(@bill.shift), class: 'btn btn-default' do %>
      <%= icon('list') %> <%= 'Back to shift' %>
    <% end %>

    <%= link_to shift_bill_url(@bill_item.bill.shift, @bill_item.bill), class: 'btn btn-default' do %>
      <%= icon('arrow-left') %> <%= 'Back to bill' %>
    <% end %>
  </div>

  <div class="text-right col-xs-12 col-md-8 action-bar-section">
  </div>
</menu>

<section class="col-xs-12 col-sm-8 col-sm-offset-2 col-md-6 col-md-offset-3">
  <%= form_for @bill_item, url: shift_bill_items_url(@bill.shift, @bill, @bill_item) do |f| %>
    <div class="form-group">
      <%= f.label :good %>
      <%= f.select :good_id, @goods.map{ |good| [ "#{good.name} #{'%.2f' % good.price}", good.id ] }, {}, { class: 'form-control' } %>
    </div>

    <div class="form-group">
      <%= f.label :quantity %>
      <%= f.select :quantity, (1..100).map{ |n| [n,n] }, {}, { class: 'form-control' } %>
    </div>

    <div class="form-group text-right">
      <%= f.submit 'Add', class: 'btn btn-primary' %>
    </div>
  <% end %>
</section>

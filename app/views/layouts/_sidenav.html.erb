<nav class="sidenav sidenav-hidden" id="sidenav">
  <ul class="sidenav-top">
    <% if user_signed_in? %>
      <li><a href='#'><%= current_user.name %></a></li>
      <% if current_user.admin? %>
        <li><%= link_to 'Admin dashboard', rails_admin_url %></li>
      <% end %>
      <li><%= link_to 'Shifts', shifts_url %></li>
      <li><%= link_to 'Sign out', destroy_user_session_url, method: :delete %></li>
    <% else %>
      <li><%= link_to 'Sign in', new_user_session_url %></li>
    <% end %>
  </ul>
  <ul class="sidenav-bottom">
  </ul>
</nav>
